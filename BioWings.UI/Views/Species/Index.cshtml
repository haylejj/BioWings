@model ApiPaginatedListResult<SpeciesGetViewModel>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/MainLayout/Index.cshtml";
}

<div class="card">
    <div class="card-header border-0 pt-6">
        <!--begin::Card title-->
        <div class="card-title">
            <!--begin::Search-->
            <div class="d-flex align-items-center position-relative my-1">
                <span class="svg-icon svg-icon-1 position-absolute ms-6">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <rect opacity="0.5" x="17.0365" y="15.1223" width="8.15546" height="2" rx="1" transform="rotate(45 17.0365 15.1223)" fill="currentColor"></rect>
                        <path d="M11 19C6.55556 19 3 15.4444 3 11C3 6.55556 6.55556 3 11 3C15.4444 3 19 6.55556 19 11C19 15.4444 15.4444 19 11 19ZM11 5C7.53333 5 5 7.53333 5 11C5 14.4667 7.53333 17 11 17C14.4667 17 17 14.4667 17 11C17 7.53333 14.4667 5 11 5Z" fill="currentColor"></path>
                    </svg>
                </span>
                <input type="text" id="searchInput" class="form-control form-control-solid w-250px ps-14" placeholder="Search Species">
            </div>
            <!--end::Search-->
        </div>
        <!--begin::Card toolbar-->
        <div class="card-toolbar">
            <div class="d-flex justify-content-end">
                <button type="button" class="btn btn-light-dark" data-bs-toggle="modal" data-bs-target="#kt_createmodal_species">
                    <span class="svg-icon svg-icon-2">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <rect opacity="0.5" x="11.364" y="20.364" width="16" height="2" rx="1" transform="rotate(-90 11.364 20.364)" fill="currentColor"></rect>
                            <rect x="4.36396" y="11.364" width="16" height="2" rx="1" fill="currentColor"></rect>
                        </svg>
                    </span>
                    New Species
                </button>
            </div>
        </div>
        <!--end::Card toolbar-->
    </div>
    <!--begin::Card body-->
    <div class="card-body py-4">
        <!--begin::Table-->
        <table class="table align-middle table-row-dashed fs-6 gy-5" id="speciesTable">
            <thead>
                <tr class="text-start text-gray-400 fw-bold fs-7 text-uppercase gs-0">
                    <th>Id</th>
                    <th>Species Name</th>
                    <th>Scientific Name</th>
                    <th>Turkish Name</th>
                    <th>Hesselbart Name</th>
                    <th>Genus Name</th>
                    <th>Kocak Name</th>
                    <th>Authority Name</th>
                    <th></th>
                </tr>
            </thead>
            <tbody class="text-gray-600 fw-semibold">
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@(item.Name ?? "-")</td>
                        <td>@(item.ScientificName ?? "-")</td>
                        <td>@(item.TurkishName ?? "-")</td>
                        <td>@(item.HesselbarthName ?? "-")</td>
                        <td>@(item.GenusName ?? "-")</td>
                        <td>@(item.KocakName ?? "-")</td>
                        <td>@(item.AuthorityName ?? "-")</td>
                        <td>
                            <div class="d-flex justify-content-center">
                                <a href="javascript:;"
                                   data-species='@Html.Raw(Json.Serialize(item))'
                                   class="btn btn-icon btn-bg-light btn-active-color-primary btn-sm me-2 view-details">
                                    <i class="fas fa-eye"></i>
                                </a>
                                <a href="javascript:;" onclick="updateSpecies(@item.Id)"
                                   class="btn btn-icon btn-bg-light btn-active-color-primary btn-sm me-2">
                                    <i class="fas fa-edit text-primary"></i>
                                </a>
                                <a href="javascript:;" onclick="confirmDelete(@item.Id)"
                                   class="btn btn-icon btn-bg-light btn-active-color-danger btn-sm">
                                    <i class="fas fa-trash text-danger"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <!--end::Table-->
        <!--begin::Pagination-->
        <div class="d-flex flex-stack flex-wrap pt-10">
            <div class="fs-6 fw-semibold text-gray-700">
                Showing @((Model.PageNumber - 1) * 25 + 1) to @Math.Min(Model.PageNumber * 25, Model.TotalCount) of @Model.TotalCount entries
            </div>
            <ul class="pagination">
                @{
                    var maxVisiblePages = 5;
                    var currentPage = Model.PageNumber;
                    var totalPages = Model.TotalPages;
                    var halfVisible = (maxVisiblePages - 1) / 2;
                    var startPage = Math.Max(1, currentPage - halfVisible);
                    var endPage = Math.Min(totalPages, startPage + maxVisiblePages - 1);
                    if (endPage - startPage + 1 < maxVisiblePages)
                    {
                        startPage = Math.Max(1, endPage - maxVisiblePages + 1);
                    }
                }

                <li class="page-item previous @(!Model.HasPreviousPage ? "disabled" : "")">
                    @if (Model.HasPreviousPage)
                    {
                        <a href="javascript:;" onclick="loadPage(@(Model.PageNumber - 1))" class="page-link">
                            <i class="previous"></i>
                        </a>
                    }
                    else
                    {
                        <span class="page-link"><i class="previous"></i></span>
                    }
                </li>

                @if (startPage > 1)
                {
                    <li class="page-item">
                        <a href="javascript:;" onclick="loadPage(1)" class="page-link">1</a>
                    </li>
                    @if (startPage > 2)
                    {
                        <li class="page-item disabled">
                            <span class="page-link">...</span>
                        </li>
                    }
                }

                @for (var i = startPage; i <= endPage; i++)
                {
                    <li class="page-item @(i == currentPage ? "active" : "")">
                        <a href="javascript:;" onclick="loadPage(@i)" class="page-link">@i</a>
                    </li>
                }

                @if (endPage < totalPages)
                {
                    @if (endPage < totalPages - 1)
                    {
                        <li class="page-item disabled">
                            <span class="page-link">...</span>
                        </li>
                    }
                    <li class="page-item">
                        <a href="javascript:;" onclick="loadPage(@totalPages)" class="page-link">@totalPages</a>
                    </li>
                }

                <li class="page-item next @(!Model.HasNextPage ? "disabled" : "")">
                    @if (Model.HasNextPage)
                    {
                        <a href="javascript:;" onclick="loadPage(@(Model.PageNumber + 1))" class="page-link">
                            <i class="next"></i>
                        </a>
                    }
                    else
                    {
                        <span class="page-link"><i class="next"></i></span>
                    }
                </li>
            </ul>
        </div>
    </div>
</div>

<!--begin::Detail Modal-->
<div class="modal fade" id="kt_modal_species" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-xl">
        <div class="modal-content rounded">
            <div class="modal-header pb-0 border-0 justify-content-end">
                <div class="btn btn-sm btn-icon btn-active-color-primary" data-bs-dismiss="modal">
                    <i class="ki-duotone ki-cross fs-1">
                        <span class="path1"></span>
                        <span class="path2"></span>
                    </i>
                </div>
            </div>

            <div class="modal-body scroll-y px-10 px-lg-15 pt-0 pb-15">
                <div class="mb-13 text-center">
                    <h1 class="mb-3">Species Details</h1>
                </div>

                <!--begin::Classification Info-->
                <div class="card shadow-sm mb-8">
                    <div class="card-header">
                        <h3 class="card-title">
                            <span class="card-label fw-bold text-dark">Classification Information</span>
                        </h3>
                    </div>
                    <div class="card-body">
                        <div class="row g-5">
                            <div class="col-xl-6">
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Species Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Scientific Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalScientificName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Family Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalFamilyName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Genus Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalGenusName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Authority Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalAuthorityName"></div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Turkish Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalTurkishName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">English Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalEnglishName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">EU Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalEUName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Full Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalFullName"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!--begin::Additional Names-->
                <div class="card shadow-sm">
                    <div class="card-header">
                        <h3 class="card-title">
                            <span class="card-label fw-bold text-dark">Additional Names</span>
                        </h3>
                    </div>
                    <div class="card-body">
                        <div class="row g-5">
                            <div class="col-xl-6">
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Hesselbarth Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalHesselbarthName"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Turkish Names Trakel</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalTurkishNamesTrakel"></div>
                                </div>
                            </div>
                            <div class="col-xl-6">
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Trakel</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalTrakel"></div>
                                </div>
                                <div class="field-container mb-5">
                                    <div class="fs-6 fw-semibold mb-2">Kocak Name</div>
                                    <div class="fs-7 text-gray-800 bg-light p-3 rounded" id="modalKocakName"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
<!--end::Detail Modal-->
<!--begin::Create Modal-->
<div class="modal fade" id="kt_createmodal_species" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered mw-650px">
        <div class="modal-content rounded">
            <div class="modal-header pb-0 border-0 justify-content-end">
                <div class="btn btn-sm btn-icon btn-active-color-primary" data-bs-dismiss="modal">
                    <i class="ki-duotone ki-cross fs-1">
                        <span class="path1"></span>
                        <span class="path2"></span>
                    </i>
                </div>
            </div>
            <div class="modal-body scroll-y px-10 px-lg-15 pt-0 pb-15">
                <form id="create_species_form" class="form">
                    <div class="mb-13 text-center">
                        <h1 class="mb-3">Create New Species</h1>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Species Name</label>
                            <input type="text" class="form-control form-control-solid" id="createName" required>
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Scientific Name</label>
                            <input type="text" class="form-control form-control-solid" id="createScientificName">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Turkish Name</label>
                            <input type="text" class="form-control form-control-solid" id="createTurkishName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">English Name</label>
                            <input type="text" class="form-control form-control-solid" id="createEnglishName">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Family</label>
                            <select class="form-select form-select-solid" id="createFamilyId">
                                <option value="">Select Family</option>
                            </select>
                            <small class="text-muted mt-2 d-block">
                                Can't find the family? <a href="/Family/Index" class="fw-bold">Add new family</a>
                            </small>
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Genus</label>
                            <select class="form-select form-select-solid" id="createGenusId">
                                <option value="">Select Genus</option>
                            </select>
                            <small class="text-muted mt-2 d-block">
                                Can't find the genus? <a href="/Genus/Index" class="fw-bold">Add new genus</a>
                            </small>
                        </div>
                    </div>
                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Authority Name</label>
                            <input type="text" class="form-control form-control-solid" id="createAuthorityName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Authority Year</label>
                            <input type="number" class="form-control form-control-solid" id="createAuthorityYear" min="1700" max="2100">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Full Name</label>
                            <input type="text" class="form-control form-control-solid" id="createFullName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">EU Name</label>
                            <input type="text" class="form-control form-control-solid" id="createEUName">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Hesselbarth Name</label>
                            <input type="text" class="form-control form-control-solid" id="createHesselbarthName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Turkish Names Trakel</label>
                            <input type="text" class="form-control form-control-solid" id="createTurkishNamesTrakel">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Trakel</label>
                            <input type="text" class="form-control form-control-solid" id="createTrakel">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Kocak Name</label>
                            <input type="text" class="form-control form-control-solid" id="createKocakName">
                        </div>
                    </div>

                    <div class="text-center">
                        <button type="button" class="btn btn-light me-3" data-bs-dismiss="modal">Cancel</button>
                        <button type="button" class="btn btn-primary" id="createSpeciesButton">
                            <span class="indicator-label">Submit</span>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<!--end::Create Modal-->
<!--begin::Update Modal-->
<div class="modal fade" id="kt_updatemodal_species" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered mw-650px">
        <div class="modal-content rounded">
            <div class="modal-header pb-0 border-0 justify-content-end">
                <div class="btn btn-sm btn-icon btn-active-color-primary" data-bs-dismiss="modal">
                    <i class="ki-duotone ki-cross fs-1">
                        <span class="path1"></span>
                        <span class="path2"></span>
                    </i>
                </div>
            </div>
            <div class="modal-body scroll-y px-10 px-lg-15 pt-0 pb-15">
                <form id="update_species_form" class="form">
                    <div class="mb-13 text-center">
                        <h1 class="mb-3">Update Species</h1>
                    </div>

                    <input type="hidden" id="updateId">

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Species Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Scientific Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateScientificName">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Turkish Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateTurkishName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">English Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateEnglishName">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Family</label>
                            <select class="form-select form-select-solid" id="updateFamilyId">
                                <option value="">Select Family</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Genus</label>
                            <select class="form-select form-select-solid" id="updateGenusId">
                                <option value="">Select Genus</option>
                            </select>
                        </div>
                    </div>
                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Authority Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateAuthorityName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Authority Year</label>
                            <input type="number" class="form-control form-control-solid" id="updateAuthorityYear" min="1700" max="2100">
                        </div>
                    </div>
                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Full Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateFullName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">EU Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateEUName">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Hesselbarth Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateHesselbarthName">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Turkish Names Trakel</label>
                            <input type="text" class="form-control form-control-solid" id="updateTurkishNamesTrakel">
                        </div>
                    </div>

                    <div class="row g-9 mb-8">
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Trakel</label>
                            <input type="text" class="form-control form-control-solid" id="updateTrakel">
                        </div>
                        <div class="col-md-6">
                            <label class="fs-6 fw-semibold mb-2">Kocak Name</label>
                            <input type="text" class="form-control form-control-solid" id="updateKocakName">
                        </div>
                    </div>

                    <div class="text-center">
                        <button type="button" class="btn btn-light me-3" data-bs-dismiss="modal">Cancel</button>
                        <button type="button" class="btn btn-primary" id="updateSpeciesButton">
                            <span class="indicator-label">Update</span>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
<!--end::Update Modal-->
@section Scripts {
    <script>
        // DataTable initialization and common functions
        var table = $('#speciesTable').DataTable({
            "searching": false,
            "paging": false,
            "info": false,
            "ordering": true,
            "order": [[0, "asc"]]
        });

        // Common utility functions
        function debounce(func, wait) {
            let timeout;
            return function executedFunction(...args) {
                const later = () => {
                    clearTimeout(timeout);
                    func(...args);
                };
                clearTimeout(timeout);
                timeout = setTimeout(later, wait);
            };
        }

        function loadPage(pageNumber) {
            const searchTerm = $('#searchInput').val().trim();
            const baseUrl = '/Species/Index';
            const queryParams = new URLSearchParams(window.location.search);

            queryParams.set('pageNumber', pageNumber);
            const currentPageSize = queryParams.get('pageSize') || '@Model.PageSize';
            queryParams.set('pageSize', currentPageSize);

            if (searchTerm) {
                queryParams.set('searchTerm', searchTerm);
            } else {
                queryParams.delete('searchTerm');
            }

            window.location.href = `${baseUrl}?${queryParams.toString()}`;
        }

        // ===============================
        // DETAIL VIEW FUNCTIONALITY
        // ===============================
        function initializeViewDetails() {
            $('.view-details').on('click', function() {
                const speciesData = $(this).data('species');
                populateDetailModal(speciesData);
                $('#kt_modal_species').modal('show');
            });
        }

        function populateDetailModal(speciesData) {
            $('#modalScientificName').text(speciesData.scientificName || '-');
            $('#modalName').text(speciesData.name || '-');
            $('#modalFamilyName').text(speciesData.familyName || '-');
            $('#modalGenusName').text(speciesData.genusName || '-');
            $('#modalAuthorityName').text(speciesData.authorityName || '-');
            $('#modalTurkishName').text(speciesData.turkishName || '-');
            $('#modalEnglishName').text(speciesData.englishName || '-');
            $('#modalEUName').text(speciesData.euName || '-');
            $('#modalFullName').text(speciesData.fullName || '-');
            $('#modalHesselbarthName').text(speciesData.hesselbarthName || '-');
            $('#modalTurkishNamesTrakel').text(speciesData.turkishNamesTrakel || '-');
            $('#modalTrakel').text(speciesData.trakel || '-');
            $('#modalKocakName').text(speciesData.kocakName || '-');
        }

        // ===============================
        // CREATE FUNCTIONALITY
        // ===============================
        $(document).ready(function() {
            initializeCreateModal();
            initializeSearchFunctionality();
            initializeViewDetails();
        });

        function initializeCreateModal() {
            $('#kt_createmodal_species').on('show.bs.modal', function() {
                populateDropdowns('create');
            });

            $('#createSpeciesButton').on('click', handleCreateSubmit);

            $('#kt_createmodal_species').on('hidden.bs.modal', function() {
                $('#create_species_form')[0].reset();
                $('.is-invalid').removeClass('is-invalid');
                $('.invalid-feedback').remove();
            });
        }

        function handleCreateSubmit() {
            if (!validateForm('create_species_form')) return;
            if (!validateAuthorityFields()) return;

            const formData = 
            {
                scientificName: $('#createScientificName').val() || null,
                name: $('#createName').val() || null,
                turkishName: $('#createTurkishName').val() || null,
                englishName: $('#createEnglishName').val() || null,
                genusId: $('#createGenusId').val() ? parseInt($('#createGenusId').val()) : null,
                familyId: $('#createFamilyId').val() ? parseInt($('#createFamilyId').val()) : null,
                authorityName: $('#createAuthorityName').val() || null,
                authorityYear: $('#createAuthorityYear').val() ? parseInt($('#createAuthorityYear').val()) : null,
                euName: $('#createEUName').val() || null,
                fullName: $('#createFullName').val() || null,
                hesselbarthName: $('#createHesselbarthName').val() || null,
                turkishNamesTrakel: $('#createTurkishNamesTrakel').val() || null,
                trakel: $('#createTrakel').val() || null,
                kocakName: $('#createKocakName').val() || null
            };
                 const submitButton = $('#createSpeciesButton');
                submitButton.attr('disabled', true);
                submitButton.find('.indicator-label').html(`
                    <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                    Creating...
                `);

            submitCreate(formData);
        }

        function submitCreate(formData) {
            $.ajax({
                url: 'https://localhost:7128/api/Species',
                type: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(formData),
                success: function(response) {
                    handleCreateSuccess(response);
                },
                error: function(xhr, status, error) {
                    handleError(xhr, 'create');
                }
            });
        }

        function handleCreateSuccess(response) {
            if (response.isSuccess) {
                Swal.fire({
                    title: 'Success!',
                    text: 'Species created successfully',
                    icon: 'success',
                    timer: 2000,
                    showConfirmButton: true
                }).then(() => {
                    $('#kt_createmodal_species').modal('hide');
                    window.location.reload();
                });
            } else {
                showError(response.errorList ? response.errorList.join(', ') : 'Failed to create species');
            }
        }

        function validateAuthorityFields() {
            const authorityName = $('#createAuthorityName').val();
            const authorityYear = $('#createAuthorityYear').val();

            if ((authorityName && !authorityYear) || (!authorityName && authorityYear)) {
                Swal.fire({
                    title: 'Validation Error!',
                    text: 'Both Authority Name and Year must be provided together.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return false;
            }
            return true;
        }

        // ===============================
        // UPDATE FUNCTIONALITY
        // ===============================
        async function updateSpecies(id) {
            try {
                const species = await fetchSpeciesData(id);
                await populateDropdowns('update');
                populateUpdateForm(species);
                showUpdateModal();
            } catch (error) {
                console.error('Error:', error);
                showError('Failed to fetch species data: ' + error.message);
            }
        }

        async function fetchSpeciesData(id) {
            const response = await fetch(`https://localhost:7128/api/Species/${id}`);
            if (!response.ok) {
                throw new Error('Failed to fetch species data');
            }
            const result = await response.json();
            return result.data;
        }

        function populateUpdateForm(species) {
            if (!species) return;

            $('#updateId').val(species.id || '');
            $('#updateScientificName').val(species.scientificName || '');
            $('#updateName').val(species.name || '');
            $('#updateTurkishName').val(species.turkishName || '');
            $('#updateEnglishName').val(species.englishName || '');
            $('#updateGenusId').val(species.genusId || '');
            $('#updateFamilyId').val(species.familyId || '');
            $('#updateAuthorityName').val(species.authorityName || '');
            $('#updateAuthorityYear').val(species.authorityYear || '');
            $('#updateEUName').val(species.euName || '');
            $('#updateFullName').val(species.fullName || '');
            $('#updateHesselbarthName').val(species.hesselbarthName || '');
            $('#updateTurkishNamesTrakel').val(species.turkishNamesTrakel || '');
            $('#updateTrakel').val(species.trakel || '');
            $('#updateKocakName').val(species.kocakName || '');

            // Family dropdown'ı için özel işlem
            if (species.familyId) {
                setTimeout(() => {
                    $('#updateFamilyId').val(species.familyId);
                }, 500); // Dropdown populate olduktan sonra değeri set et
            }
        }

        function showUpdateModal() {
            const modal = new bootstrap.Modal(document.getElementById('kt_updatemodal_species'));
            modal.show();
        }

        // Update form submission handler
        $('#updateSpeciesButton').on('click', function() {
            if (!validateForm('update_species_form')) return;
            if (!validateAuthorityFieldsUpdate()) return;

            const submitButton = $(this);
            submitButton.attr('disabled', true);
            submitButton.find('.indicator-label').html(`
                <span class="spinner-border spinner-border-sm" role="status" aria-hidden="true"></span>
                Updating...
            `);

            const formData = {
                id: parseInt($('#updateId').val()) || 0,
                name: $('#updateName').val() || null,
                scientificName: $('#updateScientificName').val() || null,
                turkishName: $('#updateTurkishName').val() || null,
                englishName: $('#updateEnglishName').val() || null,
                genusId: $('#updateGenusId').val() ? parseInt($('#updateGenusId').val()) : null,
                familyId: $('#updateFamilyId').val() ? parseInt($('#updateFamilyId').val()) : null,
                authorityName: $('#updateAuthorityName').val() || null,
                authorityYear: $('#updateAuthorityYear').val() ? parseInt($('#updateAuthorityYear').val()) : null,
                euName: $('#updateEUName').val() || null,
                fullName: $('#updateFullName').val() || null,
                hesselbarthName: $('#updateHesselbarthName').val() || null,
                turkishNamesTrakel: $('#updateTurkishNamesTrakel').val() || null,
                trakel: $('#updateTrakel').val() || null,
                kocakName: $('#updateKocakName').val() || null
            };

            submitUpdate(formData);
        });
        function validateAuthorityFieldsUpdate() {
            const authorityName = $('#updateAuthorityName').val();
            const authorityYear = $('#updateAuthorityYear').val();

            if ((authorityName && !authorityYear) || (!authorityName && authorityYear)) {
                Swal.fire({
                    title: 'Validation Error!',
                    text: 'Both Authority Name and Year must be provided together.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return false;
            }

            if (authorityYear && (authorityYear < 1700 || authorityYear > 2100)) {
                Swal.fire({
                    title: 'Validation Error!',
                    text: 'Authority Year must be between 1700 and 2100.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return false;
            }

            return true;
        }


        function submitUpdate(formData) {
            $.ajax({
                url: 'https://localhost:7128/api/Species',
                type: 'PUT',
                contentType: 'application/json',
                data: JSON.stringify(formData),
                success: function(response, status, xhr) {
                    handleUpdateSuccess(response, xhr);
                },
                error: function(xhr, status, error) {
                    handleError(xhr, 'update');
                }
            });
        }

        function handleUpdateSuccess(response, xhr) {
            if (xhr.status === 204 || (response && response.isSuccess)) {
                Swal.fire({
                    title: 'Success!',
                    text: 'Species updated successfully',
                    icon: 'success',
                    timer: 2000,
                    showConfirmButton: true
                }).then(() => {
                    $('#kt_updatemodal_species').modal('hide');
                    window.location.reload();
                });
            } else {
                showError(response?.errorList ? response.errorList.join(', ') : 'Failed to update species');
            }
        }

        // ===============================
        // COMMON HELPER FUNCTIONS
        // ===============================
        function validateForm(formId) {
            if (!$('#' + formId)[0].checkValidity()) {
                Swal.fire({
                    title: 'Validation Error!',
                    text: 'Please fill in all required fields.',
                    icon: 'error',
                    confirmButtonText: 'OK'
                });
                return false;
            }
            return true;
        }

        function showError(message) {
            Swal.fire({
                title: 'Error!',
                text: message,
                icon: 'error'
            });
        }

        function handleError(xhr, operation) {
            let errorMessage = `Failed to ${operation} species`;
            if (xhr.responseJSON && xhr.responseJSON.errorList) {
                errorMessage = xhr.responseJSON.errorList.join(', ');
            }
            showError(errorMessage);
        }

                      async function populateDropdowns(prefix) {
            try {
                await Promise.all([
                    populateDropdown(`${prefix}FamilyId`, 'https://localhost:7128/api/Families', 'Family'),
                    populateDropdown(`${prefix}GenusId`, 'https://localhost:7128/api/Genera', 'Genus')
                ]);
            } catch (error) {
                console.error('Error loading dropdowns:', error);
                Swal.fire({
                    title: 'Error!',
                    text: 'Failed to load dropdown data: ' + error.message,
                    icon: 'error'
                });
            }
        }

        async function populateDropdown(elementId, url, dropdownType) {
            try {
                const response = await fetch(url);
                if (!response.ok) {
                    throw new Error(`Failed to fetch ${dropdownType.toLowerCase()} data`);
                }

                const result = await response.json();
                const select = document.getElementById(elementId);
                if (!select) {
                    throw new Error(`Select element with id ${elementId} not found`);
                }

                // Reset dropdown
                select.innerHTML = `<option value="">Select ${dropdownType}</option>`;

                // Ensure data exists and is an array
                if (!result || !result.data || !Array.isArray(result.data)) {
                    console.warn(`No valid ${dropdownType.toLowerCase()} data received`);
                    return;
                }

                // Filter and sort items - only include items with valid names
                const sortedItems = result.data
                    .filter(item =>
                        item != null && // Remove null/undefined items
                        item.name != null && // Name must exist
                        item.name.trim() !== '' // Name must not be empty or just whitespace
                    )
                    .map(item => ({
                        id: item.id,
                        name: item.name.trim()
                    }))
                    .sort((a, b) => {
                        return a.name.localeCompare(b.name, 'tr-TR');
                    });

                // Add options to select
                sortedItems.forEach(item => {
                    const option = document.createElement('option');
                    option.value = item.id;
                    option.textContent = item.name;
                    select.appendChild(option);
                });

            } catch (error) {
                console.error(`Error in populateDropdown for ${dropdownType}:`, error);
                throw error;
            }
        }

        // Search functionality initialization
        function initializeSearchFunctionality() {
            const searchInput = $('#searchInput');

            const debouncedSearch = debounce(function(value) {
                if (value.trim().length === 0) {
                    window.location.href = '/Species/Index';
                } else {
                    const currentPageSize = new URLSearchParams(window.location.search).get('pageSize') || '@Model.PageSize';
                    window.location.href = `/Species/Index?searchTerm=${encodeURIComponent(value.trim())}&pageNumber=1&pageSize=${currentPageSize}`;
                }
            }, 500);

            searchInput.off('keyup').on('keyup', function() {
                debouncedSearch(this.value);
            });

            const urlParams = new URLSearchParams(window.location.search);
            const searchTerm = urlParams.get('searchTerm');
            if (searchTerm) {
                searchInput.val(decodeURIComponent(searchTerm));
            }
        }
    </script>
}