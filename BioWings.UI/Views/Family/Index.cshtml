@model ApiPaginatedListResult<FamilyGetViewModel>
@{
    ViewData["Title"] = "Family";
    Layout = "~/Views/MainLayout/Index.cshtml";
}

<div class="card">
    <div class="card-header border-0 pt-6">
        <div class="card-title">
            <div class="d-flex align-items-center position-relative my-1">
                <span class="svg-icon svg-icon-1 position-absolute ms-6">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <rect opacity="0.5" x="17.0365" y="15.1223" width="8.15546" height="2" rx="1" transform="rotate(45 17.0365 15.1223)" fill="currentColor"></rect>
                        <path d="M11 19C6.55556 19 3 15.4444 3 11C3 6.55556 6.55556 3 11 3C15.4444 3 19 6.55556 19 11C19 15.4444 15.4444 19 11 19ZM11 5C7.53333 5 5 7.53333 5 11C5 14.4667 7.53333 17 11 17C14.4667 17 17 14.4667 17 11C17 7.53333 14.4667 5 11 5Z" fill="currentColor"></path>
                    </svg>
                </span>
                <input type="text" id="searchInput" class="form-control form-control-solid w-250px ps-14" placeholder="Search Family">
            </div>
        </div>
        <div class="card-toolbar">
            <div class="d-flex justify-content-end">
                <button type="button" class="btn btn-light-dark" data-bs-toggle="modal" data-bs-target="#kt_createmodal_family">
                    <span class="svg-icon svg-icon-2">
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                            <rect opacity="0.5" x="11.364" y="20.364" width="16" height="2" rx="1" transform="rotate(-90 11.364 20.364)" fill="currentColor"></rect>
                            <rect x="4.36396" y="11.364" width="16" height="2" rx="1" fill="currentColor"></rect>
                        </svg>
                    </span>
                    New Family
                </button>
            </div>
        </div>
    </div>

    <div class="card-body py-4">
        <table class="table align-middle table-row-dashed fs-6 gy-5 dataTable no-footer" id="familyTable">
            <thead>
                <tr class="text-start text-gray-400 fw-bold fs-7 text-uppercase">
                    <th style="width: 100px">Id</th>
                    <th>Family Name</th>
                    <th style="width: 120px" class="text-end"></th>
                </tr>
            </thead>
            <tbody class="text-gray-600">
                @foreach (var item in Model.Items)
                {
                    <tr>
                        <td>@item.Id</td>
                        <td>@(item.Name ?? "-")</td>
                        <td>
                            <div class="d-flex justify-content-end">
                                <button onclick="confirmDelete(@item.Id)"
                                        class="btn btn-icon btn-sm btn-light-danger me-2">
                                    <i class="fas fa-trash fs-7"></i>
                                </button>
                                <button onclick="updateFamily(@item.Id)"
                                        class="btn btn-icon btn-sm btn-light-primary">
                                    <i class="fas fa-edit fs-7"></i>
                                </button>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

        @if (Model.TotalCount > 0)
        {
            <div class="d-flex flex-stack flex-wrap pt-10">
                <div class="fs-6 fw-semibold text-gray-700">
                    @{
                        var startEntry = (Model.PageNumber - 1) * Model.PageSize + 1;
                        var endEntry = Math.Min(Model.PageNumber * Model.PageSize, Model.TotalCount);
                    }
                    Showing @startEntry to @endEntry of @Model.TotalCount entries
                </div>

                @if (Model.TotalPages > 1)
                {
                    <ul class="pagination">
                        <li class="page-item previous @(!Model.HasPreviousPage ? "disabled" : "")">
                            @if (Model.HasPreviousPage)
                            {
                                <a href="javascript:;" onclick="loadPage(@(Model.PageNumber - 1))" class="page-link">
                                    <i class="previous"></i>
                                </a>
                            }
                            else
                            {
                                <span class="page-link"><i class="previous"></i></span>
                            }
                        </li>

                        @for (var i = 1; i <= Model.TotalPages; i++)
                        {
                            if (i == Model.PageNumber)
                            {
                                <li class="page-item active">
                                    <span class="page-link">@i</span>
                                </li>
                            }
                            else
                            {
                                <li class="page-item">
                                    <a href="javascript:;" onclick="loadPage(@i)" class="page-link">@i</a>
                                </li>
                            }
                        }

                        <li class="page-item next @(!Model.HasNextPage ? "disabled" : "")">
                            @if (Model.HasNextPage)
                            {
                                <a href="javascript:;" onclick="loadPage(@(Model.PageNumber + 1))" class="page-link">
                                    <i class="next"></i>
                                </a>
                            }
                            else
                            {
                                <span class="page-link"><i class="next"></i></span>
                            }
                        </li>
                    </ul>
                }
            </div>
        }
        else
        {
            <div class="text-center pt-10">
                <p class="fs-6 fw-semibold text-gray-700">No records found</p>
            </div>
        }
    </div>
</div>

<!--begin::Create Modal-->
<div class="modal fade" id="kt_createmodal_family" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded">
            <div class="modal-header pb-0 border-0 justify-content-end">
                <div class="btn btn-sm btn-icon btn-active-color-primary" data-bs-dismiss="modal">
                    <i class="ki-duotone ki-cross fs-1">
                        <span class="path1"></span>
                        <span class="path2"></span>
                    </i>
                </div>
            </div>
            <div class="modal-body scroll-y px-10 px-lg-15 pt-0 pb-15">
                <div class="mb-13 text-center">
                    <h1 class="mb-3">Create Family</h1>
                </div>
                <form id="create_family_form">
                    <div class="d-flex flex-column mb-8">
                        <label class="fs-6 fw-semibold mb-2">Family Name<span class="text-danger">*</span></label>
                        <input type="text" class="form-control" name="name" id="createName" required />
                    </div>
                    <div class="text-center">
                        <button type="button" class="btn btn-light-success" id="createFamilyButton">
                            <span class="indicator-label">Create</span>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!--begin::Update Modal-->
<div class="modal fade" id="kt_updatemodal_family" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content rounded">
            <div class="modal-header pb-0 border-0 justify-content-end">
                <div class="btn btn-sm btn-icon btn-active-color-primary" data-bs-dismiss="modal">
                    <i class="ki-duotone ki-cross fs-1">
                        <span class="path1"></span>
                        <span class="path2"></span>
                    </i>
                </div>
            </div>
            <div class="modal-body scroll-y px-10 px-lg-15 pt-0 pb-15">
                <div class="mb-13 text-center">
                    <h1 class="mb-3">Update Family</h1>
                </div>
                <form id="update_family_form">
                    <input type="hidden" id="updateId" name="id" />
                    <div class="d-flex flex-column mb-8">
                        <label class="fs-6 fw-semibold mb-2">Name<span class="text-danger">*</span></label>
                        <input type="text" class="form-control" name="name" id="updateName" required />
                    </div>
                    <div class="text-center">
                        <button type="button" class="btn btn-light-success" id="updateFamilyButton">
                            <span class="indicator-label">Update</span>
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <style>
        #familyTable {
            width: 100% !important;
        }

            #familyTable thead th {
                padding: 0.75rem;
                vertical-align: middle;
                border-bottom: 1px dashed #e4e6ef;
            }

            #familyTable tbody td {
                padding: 0.75rem;
                vertical-align: middle;
                border-bottom: 1px dashed #e4e6ef;
            }

            #familyTable tbody tr:last-child td {
                border-bottom: none;
            }

        .btn-icon.btn-sm {
            height: 30px;
            width: 30px;
            padding: 0;
            display: inline-flex;
            align-items: center;
            justify-content: center;
        }

            .btn-icon.btn-sm i {
                font-size: 0.85rem;
                line-height: 1;
            }

        .table > :not(caption) > * > * {
            background-color: transparent !important;
        }

        #familyTable {
            table-layout: fixed;
            width: 100% !important;
        }

            #familyTable thead th:nth-child(1) {
                width: 10%;
            }

            #familyTable thead th:nth-child(2) {
                width: 75%;
            }

            #familyTable thead th:nth-child(3) {
                width: 15%;
            }

            #familyTable tbody td {
                overflow: hidden;
                text-overflow: ellipsis;
                white-space: nowrap;
            }

                /* Add padding for better content spacing */
                #familyTable tbody td:nth-child(2) {
                    padding-left: 1.5rem !important;
                    padding-right: 1.5rem !important;
                }

        /* Metronic specific styling */
        .table.gy-5 td {
            padding-top: 1.25rem;
            padding-bottom: 1.25rem;
        }

    </style>
}
@section Scripts {
    <!-- Family Module Scripts -->
    <script src="~/js/family/family-datatable.js"></script>
    <script src="~/js/family/family-search.js"></script>
    <script src="~/js/family/family-create.js"></script>
    <script src="~/js/family/family-update.js"></script>
    <script src="~/js/family/family-delete.js"></script>
    <script src="~/js/family/family-modals.js"></script>
}